import React, { useState, createContext, useContext } from "react"; const TabsContext = createContext(); export const Tabs = ({ children, defaultValue, className = "", ...props }) => { const [value, setValue] = useState(defaultValue); return <TabsContext.Provider value={{ value, setValue }}><div className={className} {...props}>{children}</div></TabsContext.Provider>; }; export const TabsList = ({ children, className = "", ...props }) => <div className={`flex space-x-1 ${className}`} {...props}>{children}</div>; export const TabsTrigger = ({ children, value, className = "", ...props }) => { const { value: activeValue, setValue } = useContext(TabsContext); return <button className={`px-3 py-1.5 rounded-md text-sm font-medium transition-colors ${activeValue === value ? "bg-white text-gray-900" : "text-gray-500 hover:text-gray-700"} ${className}`} onClick={() => setValue(value)} {...props}>{children}</button>; }; export const TabsContent = ({ children, value, className = "", ...props }) => { const { value: activeValue } = useContext(TabsContext); return activeValue === value ? <div className={className} {...props}>{children}</div> : null; };
